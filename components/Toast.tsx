
// This component is a placeholder. The actual toast rendering logic
// has been moved directly into the ToastProvider in `contexts/ToastContext.tsx`
// for better encapsulation and to avoid an extra component layer.
// This file can be safely deleted.

import React from 'react';

const ToastContainer: React.FC = () => {
  return null; // All logic is in ToastProvider
};

export default ToastContainer;
